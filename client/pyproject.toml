[project]
name = "client"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bleak>=0.22.3",
]

[dependency-groups]
dev = [
    "black>=25.1.0",
    "pyright>=1.1.392.post0",
    "pytest>=8.3.5",
]
